#  Portainer server and agent setup

The following repo is a collection of docker-compose files used in a local server.

The architecture has two parts

1. Head server, running portainer server, prometheus, and grafana
2. Agent server, running portainer agent and all other containers
